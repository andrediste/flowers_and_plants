default_stages: [pre-push]
fail_fast: true
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.0
    hooks:
      - id: ruff
      - id: ruff
        args: ["check", "--select", "I", "--fix"]
      - id: ruff-format
        
      